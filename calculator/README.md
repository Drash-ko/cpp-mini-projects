# Calculator

Simple CLI calculator in C++.

## Features

- Reads two integer values from the terminal
- Supports addition, subtraction, division, and multiplication
- Uses a `switch` statement for operation selection
- Prints the full expression and result

## Run

```bash
clang++ -std=c++17 -Wall -Wextra -pedantic main.cpp -o calculator
./calculator
```
